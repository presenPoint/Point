# 이 파일을 복사해 `.env.local` 로 저장한 뒤 값을 채우세요.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_OPENAI_API_KEY=

# Google 로그인 후 돌아올 주소 (비우면 자동: 현재 origin + "/")
# 로컬에서 Vercel로 튀면 Supabase 대시보드 Redirect URLs에 아래와 동일한 줄을 추가하세요.
# VITE_OAUTH_REDIRECT_URL=http://localhost:5173/
